---
project: example
desc: example project suite
tasks:
  build:
    cmd: echo example build
    desc: run as `s2do build` or `s2do example/build`
includes:
  - child
  - env
  - tags/tags1.yml
  - tags/tags2.yml
  - depends
  - template
  - extends
