---
project: template
desc: demonstrates use of templates and parameters in command
tasks:
  retry:
    cmd: "false"
    retries: 100
    retry_ms: 10
  retry_quiet:
    cmd: "false"
    retries: 100
    retry_ms: 10
    shell: sh -ec
