---
project: template
desc: demonstrates use of templates and parameters in command
tasks:
  params:
    cmd: echo {{args.from|required}} {{args.to|default:"something_to"}}
    desc: run as s2do template/params[from=...,to=...]
